{% extends 'base.html.twig' %}

{% block title %}Éditer le Tag{% endblock %}

{% block body %}
    <h1>Éditer le Tag</h1>

    {{ include('tag/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('tag_index') }}">Retour a la liste</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
